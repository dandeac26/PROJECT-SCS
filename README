The app.py is the initial state of the application, but the final one is in /scripts/interface.py - run it with "python interface.py" 
- requirements:
  gnuplot
  pyqt5
  lcrypto
  openssl ( distribution specific )
  
  compile command for the main test script:
  gcc -pthread benchmark-passwords.c -lssl -lcrypto -o pass-benchmark
  
  
